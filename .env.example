# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Stripe (test mode keys while developing — see DEPLOY.md for the
# Dashboard/CLI steps to create the two prices below)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_SETUP=
STRIPE_PRICE_ID_SUBSCRIPTION=

# Admin — comma-separated list of emails allowed into /app (the tracker).
# Anyone else who signs in via magic link is redirected out.
ADMIN_EMAILS=rrswat00@gmail.com

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Business — shown on the pay page, success/cancel pages, and used for
# Stripe product names. Defaults to "Afterline" in code; override here or in
# Vercel if the name changes.
NEXT_PUBLIC_BUSINESS_NAME="Afterline"
